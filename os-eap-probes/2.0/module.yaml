schema_version: 1
name: os-eap-probes
version: '2.0'
description: os-eap-probes script package for python 2
execute:
- script: configure.sh
  user: '185'
packages:
      install:
          - python-enum34
          - python-requests
modules:
  install:
  - name: os-eap-probes-common
