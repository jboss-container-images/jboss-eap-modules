schema_version: 1
name: os-eap-probes
version: '3.0'
description: os-eap-probes script package for python 3
execute:
- script: configure.sh
  user: '185'
packages:
      install:
          - python3-requests
modules:
  install:
  - name: os-eap-probes-common
